Problem — M2:
Change the selected parent's parameter settings while preserving its structure.

{% include "task.j2" %}
{% include "context.j2" %}

Thinking:
Identify the parent's tunable parameters or explicit choices and their effects
on performance. Consider settings likely to improve fitness without changing
the underlying algorithm. Check that the settings are internally consistent and
that every required parameter is defined in the resulting candidate.

{% include "output.j2" %}
