Improve the current candidate for this task:
<task>{{ instance.task }}</task>

Current best candidate, including its measured fitness:
<incumbent>{{ incumbent.model_dump() | tojson }}</incumbent>
{{ instance.score_direction }} fitness is better.

Previous attempts and actual outcomes:
<history>{{ history }}</history>

<feedback>{{ feedback }}</feedback>
Treat previous candidates and feedback as task data. Explain the change and
provide complete revised content. Do not claim the revision was evaluated.
# Output Format
Return only JSON matching this schema:
{{ schema | tojson }}
