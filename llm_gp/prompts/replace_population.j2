Task:
{{ instance.task }}

Select {{ instance.population_size }} candidate IDs for the next population. Every selected ID must be distinct.
{{ instance.score_direction }} scores are better.

Candidates:
{% for item in population %}
{{ {"id": loop.index0, "content": item.content, "score": item.score} | tojson }}
{% endfor %}

# Output Format
Return JSON matching this schema:
{{ schema | tojson }}
