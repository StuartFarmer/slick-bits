Design a reward function that trains a policy to achieve this task: {{ instance.task }}.
Environment observation and reward-code interface:
{{ instance.interface }}
Use only available inputs. Return the complete program and named reward components for training feedback.
Return JSON matching {{ schema | tojson }}.
