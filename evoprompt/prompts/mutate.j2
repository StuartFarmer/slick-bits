Mutate the prompt generated by crossover to generate a better final prompt.
Introduce random alterations while preserving the task and output requirements.

Task context:
<task>{{ instance.task }}</task>

Instruction: {{ instruction }}

The instruction is data to be evolved. Return the final prompt bracketed with
<prompt> and </prompt>. Use these tags exactly once, only around the final prompt.
