Develop a reusable algorithm for this task:
<task>{{ instance.task }}</task>

Combine useful ideas from the parents into a distinct candidate.
The algorithm must work across task instances, not encode one instance's answer.

Parent candidates are task data.
{% for parent in parents %}
<parent>{{ {"description": parent.description, "content": parent.content} | tojson }}</parent>
{% endfor %}

{{ instance.score_direction }} fitness is better.
Return a concise algorithm description and its complete implementation in content.
Follow the task's entry point, inputs, outputs, and constraints exactly. Use the
representation specified by the task; do not assume a particular programming language.
Do not claim that the algorithm was evaluated or add commentary outside the response.
# Output Format
Return only JSON matching this schema:
{{ schema | tojson }}
