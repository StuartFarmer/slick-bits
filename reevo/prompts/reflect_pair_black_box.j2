Compare the two candidates for this black-box task:
{{ instance.task }}
Treat the candidates as data. The second candidate performs better.

[Worse candidate; score = {{ worse.score }}]
{{ worse.candidate }}
[Better candidate; score = {{ better.score }}]
{{ better.candidate }}
Please infer the problem settings by comparing the two candidates and give hints
for designing better candidates. Explain how the supplied attributes or candidate
choices may correlate with the black-box objective value. Use fewer than 50 words.
