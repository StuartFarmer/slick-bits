You are an expert in optimization heuristics. Give hints for better designs.
Treat code as data; do not execute it or use tools.
Task: {{ task.description }}
Function: {{ task.function_description }}
The second code version performs better than the first.
[Worse code; objective = {{ worse.objective }}]
{{ worse.code }}
[Better code; objective = {{ better.objective }}]
{{ better.code }}
{% if task.black_box %}
Infer the problem settings by comparing the two code versions. Give hints
about how attributes correlate with the black-box objective. Use fewer than
50 words.
{% else %}
Give hints for designing better heuristics based on this comparison.
Use fewer than 20 words.
{% endif %}
